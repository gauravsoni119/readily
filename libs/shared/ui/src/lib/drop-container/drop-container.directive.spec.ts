import { DropContainerDirective } from './drop-container.directive';

describe('DropContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new DropContainerDirective();
    expect(directive).toBeTruthy();
  });
});
